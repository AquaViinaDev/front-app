export const mobileMinWidth = 320;
export const mobileMaxWidth = 819;
export const tabletMinWidth = 768;
export const tabletMaxWidth = 1180;

export const MOBILE = {
  minWidth: `${mobileMinWidth}px`,
  maxWidth: `${mobileMaxWidth}px`,
};

export const TABLET = {
  minWidth: `${tabletMinWidth}px`,
  maxWidth: `${tabletMaxWidth}px`,
};
